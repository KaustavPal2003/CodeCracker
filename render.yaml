services:
  - type: web
    name: codecracker
    runtime: docker
    plan: free
    envVars:
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: "codecracker.onrender.com"
      - key: MONGO_USERNAME
        sync: false
      - key: MONGO_PASSWORD
        sync: false
      - key: REDIS_URL
        fromService:
          type: redis
          name: codecracker-redis
          property: connectionString
      - key: DATABASE_URL
        fromDatabase:
          name: codecracker-db
          property: connectionString
      - key: APP_DOMAIN
        value: "codecracker.onrender.com"
      - key: DJANGO_COLLECTSTATIC
        value: "0"

  - type: redis
    name: codecracker-redis
    plan: free
    maxmemoryPolicy: allkeys-lru

databases:
  - name: codecracker-db
    plan: free